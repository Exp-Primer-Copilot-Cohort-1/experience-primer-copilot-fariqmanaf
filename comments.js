// Create web server
// Create a web server that listens for requests on port 3000. When a request is received, it responds with a simple message.

// 1. Import the http module.
// 2. Create a server object.
// 3. Listen for requests on port 3000.
// 4. When a request is received, respond with a message.

// The server should listen for requests on port 3000. When a request is received, it should respond with the message "Hello, World!".

// The server should respond with a status code of 200 and a Content-Type header of text/plain.

// The server should respond with the message "Hello, World!".

// The server should listen for requests on port 3000.

// The server should respond with a status code of 200.

// The server should respond with a Content-Type header of text/plain.

// The message "Hello, World!" should be returned in the response